{
  "name": "lore",
  "version": "3.0.0",
  "description": "CSS compiler for the lore system",
  "scripts": {
    "build": "sass src/scss/lore.scss css/lore.css --style=expanded --no-source-map",
    "watch": "sass src/scss/lore.scss css/lore.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Birael",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}